export type GetValidScrollValueArgs = {
    newValue: number;
    scrollSize: number;
    fallbackValue?: number;
};
export declare const getValidScrollValue: ({ newValue, scrollSize, fallbackValue }: GetValidScrollValueArgs) => number;
