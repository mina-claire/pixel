import { ScrollDirection } from '../types';
import { InfiniteScroll } from '../InfiniteScroll';
export type ResetReachedThresholdArgs = {
    direction: ScrollDirection;
    offset?: number;
    hardReset?: boolean;
};
export declare const resetReachedThreshold: (ctx: InfiniteScroll, { direction, offset, hardReset }: ResetReachedThresholdArgs) => void;
