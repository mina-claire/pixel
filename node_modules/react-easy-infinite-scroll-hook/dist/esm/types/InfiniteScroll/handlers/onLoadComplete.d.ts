import { ScrollAxisName } from '../types';
import { InfiniteScroll } from '../InfiniteScroll';
export type OnLoadCompleteArgs = {
    axis: ScrollAxisName;
    loadMore: (ctx: InfiniteScroll) => void;
};
export declare const onLoadComplete: (ctx: InfiniteScroll, { axis, loadMore }: OnLoadCompleteArgs) => void;
