import { ElementRef, ScrollingContainerRef } from '../types';
export type GetScrollingContainerRefArgs = {
    ref: ElementRef | null;
    windowScroll?: boolean;
};
export declare const getScrollingContainerRef: ({ ref, windowScroll, }: GetScrollingContainerRefArgs) => ScrollingContainerRef;
